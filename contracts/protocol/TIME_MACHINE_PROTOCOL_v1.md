# Time Machine Protocol v1 (signed)

PREDICT → LOCK → REVEAL → SCORE → DIAGNOSE → UPDATE → NEXT PERIOD

Eras: DEVELOPMENT → WALK-FORWARD LEARNING → UNTOUCHED FINAL TEST

known_at = min(filed, published, exchange_timestamp); ambiguous ⇒ EXCLUDE.

Skeptic locks falsifiers only. Hash lock blobs. No price-direction-only metrics.
